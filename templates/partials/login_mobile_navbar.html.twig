<ul class="flex flex-col items-center justify-center h-full">
  <li class="mb-6">
    <a href="#" class="text-off-gray text-2xl">Accueil</a>
  </li>
  <li class="mb-6">
    <a href="/#contact" class="text-off-gray text-2xl">Contact</a>
  </li>
  <li class="mb-6">
    <div>
      {% if app.user %}
        <a href="{{ path('app_logout') }}" class="bg-dark-blue text-white py-2 px-4 rounded hover:bg-off-blue text-[16px]">Déconnexion</a>
      {% else %}
        <a href="{{ path('app_register') }}" class="bg-dark-blue text-white py-2 px-4 rounded hover:bg-off-blue text-[16px]">S'inscrire</a>
      {% endif %}
    </div>
  </li>
</ul>
